{
  "name": "thephillymojo-ws",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket server for multiplayer backgammon",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:env": "bash -lc 'set -a; [ -f ../../.env.local ] && . ../../.env.local; set +a; node index.js'"
  },
  "dependencies": {
    "next-auth": "^4.24.13",
    "ws": "^8.18.1"
  }
}
